# World.Execute-me-
Best viewed in code

Make sure you have dotnet installed, preferably 8.0.403.
You'll need dotnet-8.0

A console visualisation of World.Execute(Me); .

This is for Windows

Make a new directory "mkdir [NAME]"

move to the directory "cd [NAME]"

run "dotnet new console"
delete everything in it except for bin and obj, then move the downloaded stuff here

(you might also wanna check line 33 AND change the file path to one that leads to your .mp3)
and "dotnet add package NAudio --version 2.4.0"

run "dotnet run" via cmd.
then type "world.execute(me);"

(The computer won't show up at first so just finish typing "world.execute(me);")

